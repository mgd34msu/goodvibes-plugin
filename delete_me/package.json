{
  "name": "rate-limiter",
  "version": "0.1.0",
  "description": "TypeScript rate limiter library supporting sliding-window, token-bucket, and fixed-window strategies",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
