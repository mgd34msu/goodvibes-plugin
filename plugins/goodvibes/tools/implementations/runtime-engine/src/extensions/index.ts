/**
 * Extensions barrel — Layer 2 type extensions.
 *
 * Re-exports all source-specific event types and specialised trigger types
 * that extend the Layer 1 base schemas from core/types.ts.
 */

// ─── Event Extensions ─────────────────────────────────────────────────────────

export * from './events/index.js';

// ─── Trigger Extensions ───────────────────────────────────────────────────────

export * from './triggers/index.js';
