/**
 * Compiled JavaScript version of ts-module.ts
 *
 * This file simulates the compiled output that would be in a dist folder.
 */

export function distFallbackFunction(x) {
  return {
    computed: x * 4,
    timestamp: Date.now(),
  };
}
