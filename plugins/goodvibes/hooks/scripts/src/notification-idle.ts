/* v8 ignore file */
/**
 * Notification Idle Hook Entry Point
 *
 * This is a thin entry point that re-exports from the shared module.
 * The actual implementation lives in src/shared/notification-idle.ts
 */

// Re-export and execute the notification idle hook
import './shared/notification-idle.js';
