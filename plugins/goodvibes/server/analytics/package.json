{
  "name": "goodvibes-analytics-server",
  "version": "2.0.0-alpha.1",
  "private": true,
  "description": "Runtime-only dependency manifest for the committed goodvibes-analytics server bundle. sql.js does not bundle cleanly; it installs in the background on first run (durable home under ~/.claude/.goodvibes/deps) — no postinstall chain.",
  "dependencies": {
    "sql.js": "^1.12.0"
  }
}
