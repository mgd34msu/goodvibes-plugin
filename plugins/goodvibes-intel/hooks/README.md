# goodvibes-intel hooks (scaffold placeholder)

Plain `.mjs` hooks, no build step (§7 R8). Authored in lane 7:
SessionStart context-gather, Setup run-once, SubagentStart pointers, PostToolUseFailure.
Every hook starts with a v1-detection check and yields to v1 when present (R16).
