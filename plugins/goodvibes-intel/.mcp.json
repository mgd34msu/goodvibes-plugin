{
  "mcpServers": {
    "goodvibes-intel": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/index.cjs"],
      "env": {
        "PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}",
        "NODE_ENV": "production"
      }
    }
  }
}
