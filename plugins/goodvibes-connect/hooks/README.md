# goodvibes-connect hooks (scaffold placeholder)

Plain `.mjs` hooks, no build step (§7 R8/R9). Authored in lane 5 (§2.4):
SessionStart open-mode announcement (connect-owned), secrets-commit-guard
(only if rebuilt warn-first with tests). Every hook yields to v1 when present (R16).
