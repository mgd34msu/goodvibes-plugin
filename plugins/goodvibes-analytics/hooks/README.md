# goodvibes-analytics hooks (scaffold placeholder)

Plain `.mjs` hooks, no build step (§7 R8). Authored in lane 6/7 (§2.4):
SessionEnd flush (slim), Stop, SubagentStop telemetry-only, PreCompact observe-only.
Every hook yields to v1 when present (R16).
