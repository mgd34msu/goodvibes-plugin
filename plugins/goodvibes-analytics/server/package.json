{
  "name": "goodvibes-analytics-server",
  "version": "2.0.0-alpha.1",
  "private": true,
  "description": "Runtime-only dependency manifest for the committed goodvibes-analytics server bundle. TUI and WASM deps that do not bundle cleanly install on first run via the Setup hook / plugin command (explicit consent) — no postinstall chain.",
  "dependencies": {
    "ink": "^5.2.0",
    "react": "^18.3.1",
    "react-devtools-core": "^4.28.5",
    "yoga-wasm-web": "^0.3.3",
    "sql.js": "^1.12.0"
  }
}
