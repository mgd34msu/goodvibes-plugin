{
  "name": "@goodvibes/connect",
  "version": "2.3.3",
  "description": "GoodVibes connect MCP server — registered HTTP + database access under a trust boundary (3 tools). Alpha scaffold.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@goodvibes/core": "*",
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  }
}
