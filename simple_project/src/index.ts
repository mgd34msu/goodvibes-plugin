/**
 * simple-project — barrel export.
 * Re-exports all public symbols from math utilities.
 */
export { add, subtract, multiply, divide, clamp, lerp } from './math.js';
export * from './arrays.js';
