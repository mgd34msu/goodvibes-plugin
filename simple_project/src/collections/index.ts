/**
 * Collection utilities barrel export.
 */
export { chunk, flatten, unique, intersection, difference, zip, groupBy } from './arrays.js';
export { pick, omit, deepClone, merge, isEmpty } from './objects.js';
